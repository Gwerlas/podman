---
- name: Verify role without any options
  hosts: all
  gather_facts: false
  tasks:
    - name: Hello world
      containers.podman.podman_container:
        name: test
        image: hello-world
        detach: false
