---
packages:
  podman:
    - slirp4netns
    - fuse-overlayfs
    - rootlesskit
    - podman
  rootless:
    - acl

default_containers_config:
  containers:
    log_driver: journald
  engine:
    cgroup_manager: cgroupfs
