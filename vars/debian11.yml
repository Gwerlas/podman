---
packages:
  podman:
    - slirp4netns
    - fuse-overlayfs
    - rootlesskit
    - podman
  rootless:
    - acl

_podman_containers_defaults:
  containers:
    log_driver: journald
  engine:
    cgroup_manager: cgroupfs
